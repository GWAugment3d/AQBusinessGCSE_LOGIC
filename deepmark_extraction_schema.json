{
  "schema_name": "deepmark_extraction_schema",
  "schema_version": "2026-01-28.1",

  "required_top_level_keys": [
    "extractedAnswers",
    "transcriptRaw"
  ],

  "notes": "DeepMark internal extraction contract. This is NOT JSON Schema (draft-2020-12). It is a deterministic validation contract used by the extraction pipeline.",

  "paper_meta": {
    "optional": true,
    "fields": [
      "student_name",
      "student_year",
      "test_title",
      "test_date"
    ]
  },

  "extractedAnswers": {
    "type": "object",
    "description": "Map of question IDs to extracted answer payloads.",
    "qid_pattern": "^[0-9]{2}\\.[0-9]{1,2}$",

    "answer_object_required_keys": [
      "qid",
      "text",
      "source",
      "wordCount",
      "charCount"
    ],

    "source_enum": [
      "transcript",
      "ocr",
      "manual",
      "unknown"
    ]
  },

  "transcriptRaw": {
    "type": "string",
    "description": "Raw transcript used for extraction. May include timestamps, filler words, and section markers."
  }
}
